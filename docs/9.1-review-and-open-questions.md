# 9.1 — Understanding Summary and Consolidated Open Questions

This is the output of running `9.0-master-prompt.md` steps 1–2 directly against the (now
REMIS-aligned, see `CHANGELOG.md`) doc set, rather than pasting the prompt into a fresh
conversation. Steps 3–5 of `9.0` continue in `system-design.md`, `architecture.md`,
`testing-strategy.md`, and this file's closing section.

---

## 1. Understanding Summary

**PCC-EDMS** is a single platform shared by two offices with distinct legal/procedural
obligations that happen to need the same infrastructure:

- **DPO** owns two modules: **DPREQ** (Module 1 — pre-research data privacy clearance) and
  **DPNDA** (Module 2 — e-signed NDAs for OJTs/trainees/community service). Both are
  single-approval-chain workflows: an applicant submits, DPO staff screens and reviews, DPO
  issues a clearance or signs off. DPNDA additionally tracks *placement* data (school,
  department, grade level, dates) that's the backbone of most DPO reports — not just a signed
  PDF.

- **ORD, via REC,** owns **REMIS** (Module 3), which is structurally heavier than DPREQ/DPNDA:
  a researcher's application must clear a **four-step academic endorsement chain** (Adviser →
  Program Head → Dean) *before* it ever reaches the Ethics Secretariat for administrative
  screening, then goes through risk-tiered review (Expedited/Committee/Full Board, per
  classification), a four-way decision (Approved / Approved with Conditions / Deferred /
  Disapproved), ongoing **monitoring** once approved, an explicit **Completion Module** to
  close it out, and a standalone **Incident Reporting** pathway (`3.5`, newly added — see below)
  that can fire at any point during monitoring, independent of the main status lifecycle.

- **4.x (shared infrastructure)** is what makes "single platform" true rather than aspirational:
  one user/role model, one file-naming and repository convention, one e-signature and
  notification mechanism, one audit trail. Every module writes into these rather than
  maintaining its own.

- **5.x (reporting)** is downstream of everything above — every report is defined as "read this
  field from that module's records." The reports are only as reliable as the underlying data
  model, which is why `5.3`'s note about needing a first-class `Placement` entity (not just a
  signed NDA file) mattered enough to carry into `system-design.md`.

**The DPO ↔ REMIS integration point** (this is the piece the original request specifically
asked about): the two offices are otherwise independent, but a REMIS incident of type **Data
Breach** or **Confidentiality Breach** (`3.5`) now explicitly notifies **DPO Staff**, because a
privacy breach inside a REC-cleared study is a DPO matter under the Data Privacy Act regardless
of which office's system it surfaced in. This is modeled as a single cross-module edge in
`knowledge-graph.json` (`entity.incident --NOTIFIES--> role.dpo_staff`) rather than a merge of
the two modules — DPREQ/DPNDA and REMIS keep separate ownership, statuses, and approval chains;
they share infrastructure and this one notification path, not their workflows.

**What changed in this pass, and why it mattered:** the original `3.1`–`3.4` were a reasonable
draft written *before* `REMIS_Functional_Requirements_Specification.md` was available, and
diverged from it in ways that weren't cosmetic — the endorsement chain, the Ethics
Secretariat/Reviewer/Chair role split, and the entire Incident Reporting module were absent.
Building the architecture on the old draft would have produced a system that couldn't represent
how REMIS actually needs to work. Full diff in `CHANGELOG.md`.

---

## 2. Consolidated Open Questions (by file)

Legend: 🔴 still open (needs DPO/ORD/IT input) · 🟢 resolved this pass (FRS-confirmed, no
longer an assumption) · 🟡 resolved by architectural decision (see `architecture.md`)

### `0.1-purpose-and-scope.md`
- 🟢 Out-of-scope list (budget/HR/LMS/public portal) — confirmed 2026-07-06: nothing on that
  list is secretly in scope.
- 🟢 Success criteria (100% paperless, all NDAs e-signed) — confirmed 2026-07-06 as the right
  go-live targets.

### `0.2-stakeholders-and-roles.md`
- 🟢 REMIS role list and capability matrix — now sourced directly from FRS §II, no longer an
  assumption.
- 🟢 DPO-side role list — confirmed 2026-07-06: **DPO Approver is retired as a separate role.**
  DPO has one role, `dpo_staff`, which owns the DPO track end to end — screening, endorsement,
  and final approval. The former `dpo_approver`-exclusive `approve()` capability now belongs to
  `dpo_staff`. Also confirmed: `student_teacher` is not a distinct trainee category PCC tracks
  separately from OJT — the `student_teacher` role and the standalone "Student Teachers by Grade
  Level" report were both removed; those trainees are now recorded as ordinary internal/external
  OJT placements. See `CHANGELOG.md` for the itemized code diff.

### `1.1`–`1.3` (DPREQ)
- 🟢 Exact form fields — confirmed 2026-07-06, **superseded 2026-07-07**: see the "Unified
  Research Ethics and Data Privacy Clearance Application Form" note in §2b below — the
  requester's 2026-07-06 "fine as-is" answer no longer stands now that a materially different
  unified form has been shared, pending confirmation of scope/timing.
- 🟡 Accepted file sizes — **revised 2026-07-07: 50MB** (was confirmed at 10MB on 2026-07-06,
  superseded one day later). Implemented in `DpndaRecordController::uploadEvaluationReport()`,
  `RemisApplicationController::submitProgressReport()`/`submitCompletionReport()` (`max:51200`),
  and the local PHP install's `upload_max_filesize`/`post_max_size` (50M/60M — the Laravel-side
  validation rule alone doesn't help if PHP itself rejects the upload first; **2026-08-31 audit
  correction:** the cited `C:\xampp\php\php.ini` is not the active one — this machine runs
  Laragon, so the active file is the Laragon PHP build's `php.ini`).
- 🟢 Retention schedule — answered 2026-07-07 (HANDOFF Part K) and **implemented 2026-07-25**:
  **7 years for issued clearances, 3 years for rejected/inactive records**, aligned with RA 10173.
  Configurable in `config/retention.php`; swept monthly by `edms:apply-retention` in report-only
  mode (disposal needs both `--purge` and `RETENTION_PURGE_ENABLED=true`). DPREQ/DPNDA do not
  differ. **Still needs DPO sign-off before purging is enabled in production** — see HANDOFF.
- 🟢 DPO screening checklist — confirmed 2026-07-06: the current 5-item checklist is fine as-is.

### `2.1`–`2.2` (DPNDA)
- 🟢 NDA template fields (Form 2 and Form 5) — confirmed 2026-07-06, **superseded 2026-07-07**:
  "Ignore all forms sent before. We will update all forms and send the approved ones." The
  `reqs/DPO EFORM 2/5 SAMPLE.pdf` files this build was based on are no longer treated as
  authoritative — updated, approved versions are coming from DPO/ORD. No code change yet (the
  new versions haven't arrived), but don't do further form-field work against the old samples.
- 🟢 Multi-version NDA template management — decided against for now (2026-07-04): the current
  hardcoded Blade template is stable and this would add moving parts for an unconfirmed need.
  Revisit if DPO/Legal later confirm the NDA text needs to change over time.

### `3.1`–`3.5` (REMIS)
- 🟢 Form fields (Sections A–F), screening checklist, decision options, endorsement procedure,
  tracking number format, monitoring/completion fields, incident types — all now FRS-sourced
  and confirmed.
- 🟢 Exact risk-classification criteria thresholds — confirmed 2026-07-07: **deliberately not
  algorithmic.** No auto-classification will be built; the assigned Ethics Reviewer picks a
  level (Minimal/Moderate/High → Expedited/Committee/Full Board Review) via a decision control
  and a required written rationale, entirely on their own judgment. This is already exactly how
  `RemisApplicationController::submitReview()` + `RiskClassification` work today — no code
  change needed. Also confirmed: a higher tier (e.g. Full Board Review) carries no automatic
  downstream consequence beyond the label itself (no extra required signatures, no different
  decision options) — `review_type` is stored but nothing branches on it in
  `RemisWorkflowService`.
- 🟢 Monitoring report cadence — confirmed 2026-07-04: every month (most studies here finish in
  3-4 months, so a longer cadence would rarely fire). Implemented in
  `RemisMonitoringService::monitoringDueDate()`, backing the ORD dashboard's Overdue Monitoring
  widget.
- 🟢 Discontinued/Withdrawn as a distinct early-exit path from Completion — decided against
  (2026-07-04): a stalled study can just stay in `monitoring` indefinitely; no distinct workflow
  branch was requested. `completion_reports.final_outcome` keeps the enum values for schema
  forward-compatibility only.
- 🟢 Multi-reviewer consolidation (FRS §VIII/step 8 — Chair "assigns reviewers" and "consolidates
  reviewer recommendations," both plural) — confirmed 2026-07-04: build panel review rather than
  the single-reviewer-per-application simplification the initial build shipped with. See
  `docs/HANDOFF.md` Part G.
- 🟢 Incident filing — who can file confirmed 2026-07-07: the researcher, Ethics Secretariat,
  Ethics Reviewer, and Ethics Committee Chair, exactly matching the existing
  `RemisApplicationPolicy::file()` implementation — no code change needed. 🔴 Whether Data
  Breach/Confidentiality Breach should auto-trigger a monitoring hold was not explicitly
  re-answered this pass (the answer addressed the who-can-file list, not this sub-question) —
  still open; current behavior remains no automatic hold, a Committee decision either way.

### `4.1`–`4.4` (Shared infrastructure)
- 🟢 Bulk role import for OJT batches — confirmed 2026-07-04, reconfirmed 2026-07-06: required,
  and built the same day (`docs/HANDOFF.md` Part H) as a CSV upload extending the Part F Admin
  UI.
- 🟡 Virus scanning on upload — confirmed 2026-07-06: deferred by stakeholder decision until the
  project is otherwise complete, not an open IT question in the meantime.
- 🟢 **E-signature capture method** — resolved 2026-07-03 (ADR-005, `architecture.md`): hybrid
  approach shipped. Typed name + timestamp remains the legally-operative record under RA 8792;
  a `signature_pad` canvas drawing is captured alongside it as a cosmetic addition, not a
  replacement.
- 🟢 SMS notifications for time-sensitive DPO whereabouts alerts — decided against (2026-07-04):
  the project is meant to run entirely free apart from deployment hosting, and every real SMS
  gateway (Twilio, etc.) is a paid service. Email (built, `docs/HANDOFF.md` Part E) is the one
  FRS-required channel; SMS stays unbuilt by design, not by gap. `notifications.channel` keeps
  the `sms` enum value for schema forward-compatibility only, in case this changes later.
- 🟢 Audit trail read access scope — confirmed 2026-07-06: Admin, **DPO Staff** (not DPO
  Approver — that role no longer exists), and REC Chair. No implementation change needed: no
  code currently restricts audit-trail read access at all (this was an open design question, not
  a built gate); this closes it as "that access list is correct, once/if it's built."
- 🔴 Whether "record viewed" (not just "record changed") events should be logged for all records
  or sensitive ones only — still open.

### `5.1`–`5.3` (Reports)
- 🟢 Whether DPO's and ORD's "Compliance Monitoring Report" should be unified — confirmed
  2026-07-06: **stay separate**, despite the shared name. No change needed; already built as two
  distinct report definitions.
- 🟢 "OJT/student teacher whereabouts" — confirmed 2026-07-06 on two points: (1) a
  placement-schedule snapshot is sufficient, no real-time check-in needed — already built this
  way; (2) "student teacher" is not a category distinct from OJT, so the report (and the
  `student_teacher` role) is just "Trainee Whereabouts" now, covering OJT placements. The
  separate "Student Teachers by Grade Level" report was removed. Reconfirmed 2026-07-07 (snapshot
  approach "will do, not real time").
- 🟢 Incident Summary report — added this pass, not an open question anymore, just new.

### `7.0-deployment.md`
- 🔴 All items in the deployment checklist remain genuinely undecided (hosting type, domain,
  SSL method, DB hosting, backup schedule, CI/CD) — these are institutional/IT decisions this
  pass cannot resolve, only build toward without blocking on.

### `8.0-tech-stack.md`
- 🟡 Inertia.js vs. separate SPA+REST — resolved in `architecture.md`.
- 🟡 Authentication approach (standalone vs. institutional SSO) — resolved in `architecture.md`
  as a default. 🟢 SSO provider — **confirmed 2026-07-06**: PCC uses Microsoft 365, with
  institutional email in the form `[name]@pccnet.edu.ph`, so Microsoft Entra ID (Azure AD) is
  the confirmed integration target (no longer just the requester's recollection). Still 🔴
  pending before SSO code can be wired up and tested end-to-end: IT registering an app in Entra
  ID and issuing a client ID/secret/tenant ID — that provisioning step is unchanged by this
  confirmation.
- 🟡 Database engine — resolved in `architecture.md`.
- 🟡 Object storage — resolved in `architecture.md` as a default pending `7.0`'s hosting
  decision.

---

## 2a. DPO ↔ Ethics Integration (added 2026-07-02, second pass)

`reqs/` (4 EVP-approved DPO EForm PDFs, previously not incorporated into this doc set) revealed
that DPREQ and REMIS are not independent applications each producing their own clearance — they
are two internal review tracks acting on one shared application (Form 1), converging on one
dual-signed clearance certificate (Form 3). Full model: `0.4-dpo-ethics-integration.md`.
*(2026-08-31 audit: superseded — since 2026-07-25 the tracks issue INDEPENDENT certificates;
see `0.4` §Issuance rule.)*
Confirmed directly by the requester, not an assumption. This changed `1.1`–`1.3`, `2.1`–`2.2`,
`3.1`, `3.3`, and `system-design.md` §3/§4 — see `CHANGELOG.md` for the itemized diff.

New open questions this raised:
- 🔴 Does every Form 1 submission require the *full* REMIS track (endorsement chain → Ethics
  Secretariat screening → risk classification → committee review), or only some studies? Put off
  for now (2026-07-06) — still assumed "every submission, both tracks" until ORD/Ethics
  Secretariat confirms otherwise.
- 🟢 Is Form 3's "Research Ethics Head" the same role as the FRS's "Ethics Committee Chair," or a
  distinct, more senior signatory? **Confirmed 2026-07-06: same person**, two labels for one
  role. No code change needed — already modeled this way.
- 🟢 Does Form 2 (Research Team NDA) apply to every research application with human respondents,
  including solo researchers, or only team submissions? **Confirmed 2026-07-06: yes, required
  for all**, including solo researchers. No code change needed — already built this way.

## 2b. Unified Application Form (pending confirmation, added 2026-07-07)

`reqs/July-7-2026_Unified-Research-Ethics-and-Data-Privacy-Clearance-Application-Form.pdf` — a
new intake form combining the DPO and Ethics application into one document (Parts I–VII
applicant-facing, Part VIII ORD review, Part IX now-named "Office for Data Privacy" (ODP) review,
Part X joint institutional clearance) — was shared alongside these notes from the requester:

1. "We'll have separate application forms for students and employees" — this unified form is not
   itself final; at least one more variant (or two) is still coming.
2. "The REC application form will consider all fields in the final version of the REC application
   form I have uploaded today" — i.e. this PDF's Parts I–VII are confirmed as the *content* the
   REC-facing form will eventually carry, even though the final packaging (student vs. employee
   variants) isn't done.
3. "We'll give you the layout of the REC Clearance and Exemption certificate within this week" —
   a Form 3 replacement is coming, and it introduces a concept the current build doesn't have:
   **Exemption** as a decision outcome (see below).

**Notable deltas from the current build**, not yet implemented pending the above:
- New Part II fields not currently on `dpreq_applications`/`research_applications`: Type of
  Research (Undergrad/Graduate/Faculty/Institutional/Action/Extension/Others), Funding Source as
  a structured pick-list (current `funding_source` is free text).
- Part III/IV restructure `respondents`/`target_population`/`vulnerable_population` into
  checkbox-driven fields (Target Participants, an 8-item Ethical Considerations Yes/No/N/A
  checklist, a 4-level self-reported risk band with an explain field) — richer than today's free
  text.
- Part V turns `data_types`/`data_collection_method` into a fuller structured set: a Classification
  of Data field using the actual Data Privacy Act categories (Non-Personal / Personal /
  Sensitive Personal / Privileged Information — the current schema has no equivalent), plus new
  storage method / who-has-access / retention period / disposal method fields (this is the
  *study's own* data handling plan, distinct from the DPO's document-retention-schedule open
  question above — don't conflate the two).
- Part VI's attachments checklist (10 named documents, including a new Data Privacy Notice,
  Data Management Plan, and Turnitin/Plagiarism Report) is more specific than the current 4-item
  upload set.
- Part VIII's Ethics Review Result adds **"Exempted"** as a fifth option alongside the FRS's
  existing four (Approved / Approved with Conditions / Deferred / Disapproved) — `decisions`
  has no such value today.
- Part IX renames the risk tiers to **Minimal/Moderate/High Risk** directly (matching what's
  already built — `RiskClassification.level` already uses exactly these three values) and adds
  an explicit **DPIA Required? Yes/No** field, which doesn't exist as a column today (DPIA is
  currently just a possible attachment, not a formal flag). It also frames risk classification
  and a full parallel "Privacy Review Result" (Approved/Approved with Revisions/Deferred/
  Disapproved) as an **ODP** (Office for Data Privacy) function — worth clarifying with the
  requester whether "ODP" is the same office as "DPO" under a new name, or a distinct one, since
  every other doc in this set calls it DPO.
- Part X's joint clearance (Cleared for Implementation / Not Cleared + both offices' signatures)
  matches the existing `clearance_certificates` dual-signature concept, just presented as one
  section of the same form rather than a separate document.

**Status update (2026-08-31 audit):** this is no longer "not started." Since this section was
written: the REC Clearance and Exemption certificate layouts ARRIVED (`reqs/REMIS-certs/`,
stakeholder 2026-07-31) and were implemented to match (`remis-clearance.blade.php`,
`remis-exemption.blade.php`); **"Exempted" is now a real decision outcome** (migration
`2026_07_28_000002_add_ethics_exemption.php`, `decisions.outcome`, exemption PDF job); and
unified-form intake fields were added (`2026_07_28_000001_add_form1_intake_fields.php`).
The field-level deltas listed above that are NOT yet implemented remain open, and the final
student/employee form variants are still pending — do not build the rest against this PDF
without confirmation.

## 3. Architecture and Data Model Confirmation Gate

Per `9.0` steps 3–4: `architecture.md` and `system-design.md` (this same session) propose the
system architecture, data model, and API/module boundaries, resolving the 🟡 items above with
documented rationale. **Per `9.0`'s own instruction, implementation should not begin until
those are reviewed and confirmed** — the 🔴 items don't need to block architecture/data-model
work, since none of them change entity shapes or module boundaries, but several (e-signature
method, SSO availability, risk-classification thresholds) do affect what gets built inside
those boundaries and are worth resolving before Module-1 implementation starts.
